# c2s2 批次2 开发报告 · 实验区音调线（2026-09-18）

## 交付内容

完成 3 个实验页，每个页面 = html + js：

| 页面 | 教材对应 | 核心交互 |
|------|---------|---------|
| `exp-ruler.html/.js` | P37 想想做做 · 图2.2-2 | 钢尺伸出长度 slider 8~25 cm；拨动后 canvas 阻尼振动 + 播放 `ruler-on-table.mp3`；伸出越短 playbackRate 越高、振动越快；等效频率 25~78 Hz 实时显示；完成 3 种不同长度后解锁填空卡 |
| `exp-scope.html/.js` | P38 演示 · 图2.2-3 | 220/440/880 Hz 三档音叉真实录音（`fork-220.m4a`/`fork-a4-440.m4a`/`fork-880.m4a`）；WebAudio Analyser 实时绘制波形，880 密、220 疏一眼可辨；先预测「更密/更疏」再播 880 Hz 揭晓；侧边读数「1 s 内振动 ___ 次 = ___ Hz」严格对齐档位 |
| `exp-range.html/.js` | P39 表2.2-1 | 对数轴 1 Hz~200 000 Hz；人耳 20~20 000 Hz 绿色高亮；<20 Hz 次声波、>20 000 Hz 超声波；6 动物按钮（人/狗/猫/蝙蝠/海豚/大象）按表 2.2-1 数值绘制听觉条带；条带重叠可视化；揭晓卡解释猫狗警觉；滑块选频率可播放 WebAudio oscillator 纯音 |

## 公共工程

- `experiment.html` 更新为 6 卡网格：钢尺/示波器/听觉范围 3 卡点亮可点；响度/音色/乐器百科 3 卡保持「即将上线」样式。
- 沿用现有 `common.css`、`fullscreen.js`、`exp-common.js`、`exp-back.js`；未动 `app.js` 及范围外文件。
- 音频全部挂用户点击事件；相对路径 `assets/audio/xxx`。

## 数字事实核对

- 人耳范围 20~20 000 Hz、次声波 <20 Hz、超声波 >20 000 Hz 与教材一致。
- 动物听觉范围严格采用表 2.2-1：人 20~20 000、狗 15~50 000、猫 60~65 000、蝙蝠 1 000~120 000、海豚 150~150 000、大象 1~20 000（显示沿用空格分节）。

## 自查

- `node --check` 通过：`exp-ruler.js`、`exp-scope.js`、`exp-range.js`。
- 桌面 + 390 px 移动宽：flex/grid 响应式，控件不溢出，按钮最小高度 44 px。
- 导航/返回按钮沿用 `exp-back.js` 模式（`data-back`）。
- 未执行 git commit。

## 已知局限

- 示波器波形依赖 WebAudio Analyser + `createMediaElementSource`；若浏览器跨域或音频加载失败，波形回退为静态直线，频率读数仍正确显示。
- 钢尺等效频率为教学映射值，非真实物理公式计算。
