# c1s4 讲解页动画修复报告（第2批）

## 修改文件
- `/Users/personal/projects/keben_web/content/physics_g8_v1_c1_s4/explain.html`

## 修改内容
1. **超声波测距动画重做**
   - 发射器/接收器改为圆柱形探头（底座+网格面+指示灯）。
   - 点击「发射脉冲」：光点慢速往返（约1.8s），去程/回程分别渐显路径。
   - 光点回到探头后，两条路径上分别标注字母 **s**。
   - 去掉「重播」按钮；再次点击发射会清掉重来。
   - 动画区内嵌联动：t 输入单位改为 ms，默认值 2.73ms → 0.46m，输入即算 s = 340×t/2000。

2. **区间测速动画重做**
   - 页面打开即完整呈现（默认值 s=8km、v=100km/h）。
   - 汽车从左驶入，经摄像头A时A闪光并开始记录路径，经B时B闪光并停止，出画后路径标 **s**。
   - 摄像头画成立杆+横臂+枪机+闪光灯样式。
   - 保留联动计算 km/h→min，新增「重新演示」按钮。

## 自查结果
- `head -3` 见 `<!DOCTYPE html>`
- `tail -2` 见 `</body></html>`
- `grep -c "</html>"` = 1
- 内联 JS 提取后 `node --check` 通过

## 完成判定
- [x] 超声波动画：逼真探头+双路径+双s标注+无重播按钮+内嵌联动
- [x] 区间测速：打开即完整+AB闪光+路径记录+出画标s
- [x] 三重自查通过
- [x] node --check 通过
- [x] 报告已写
