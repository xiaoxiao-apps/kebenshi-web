# c2s2 第二轮修复 G2b 批报告（图2.2-4 重绘）

- 日期：2026-09-18
- 范围：`content/physics_g8_v1_c2_s2/explain.html` 唯一文件，仅 Tab2「图2.2-4」SVG 区块（含其 `<defs>`）；返回按钮、exp-back.js、图注及其他内容一字未动
- 反馈：蔡总「音叉和乒乓球的图的渲染不好看，重新按照书上图2.2-4的原图来渲染」

## 改动内容

旧图问题：带铁架台（底座/立柱/横梁 rect，书上原图没有）、音叉为细线感、球为纯色圆。

重绘要点（左右双状态布局与中文标注保留）：
- **删铁架台**：三个 rect 全部移除。
- **音叉**：单一闭合 path 的 U 形双叉（叉臂宽 11、叉尖 r5.5 圆头、底部外弧 r24/内弧 r13 圆弧弯）+ 梯形过渡座 + 圆柱长柄（rx5 圆头），`linearGradient` 灰银五段渐变（tf-metal 竖向件 / tf-metal-h 备用横向件）+ 叉臂白色高光条 + #5f5f5f 描边圆润接合（stroke-linejoin=round）；尺寸占画面主体（高约 137/220）。
- **乒乓球**：`radialGradient`（ball-g，白→微橙）圆球 r13 + 细实线悬绳自上方吊下（绳顶小锚点圆），静止位球体贴音叉左臂。
- **弹开状态**：弹开位画灰色虚线圆（球轮廓虚线），静止位→弹开位画红色弧形虚线轨迹（沿绳长 r75 圆弧，带 tf-ar 箭头）；左「幅度小」（弹开约 28px）、右「幅度大」（弹开约 68px），对比明显。
- 背景保持卡片底色 #faf8f5；标注文字位置均避开图形，不压图。
- 旧 marker `ar1` 已删除，新 defs id：tf-metal / tf-metal-h / ball-g / tf-ar，全页无 id 冲突（grep 验证 ar1 残留 0、铁架台残留 0）。

## 教材事实核对

对照 `docs/c2s2-transcript.md` 第 81–85 行：球「系在细绳上」、音叉「轻触」球、重敲弹开幅度增大——重绘与原文一致，事实未改。

## 验证

- 5 个 SVG 全部 XML parse 通过。
- `python3 -m http.server 8711` 起服务，浏览器实测 Tab2 切换正常、图形渲染正确（中途修正过一次音叉朝向：首版圆拱误置顶部，已改为叉臂朝上、圆弧弯在底连柄的标准音叉形）。
- 截图：`docs/c2s2-qa/r2-g2b-desktop.png`（桌面宽）、`docs/c2s2-qa/r2-g2b-390.png`（390px 移动宽）。
- 服务已 `kill -9 $(lsof -ti:8711)` 关闭。
- 未 commit、未 push、未动其他文件。
