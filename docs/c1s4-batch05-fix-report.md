# c1s4 第05批修复报告

## F1 exp-slope.js：动画改为匀加速

**修法**
- `BASE_DURATIONS` 由 `[2.8, 1.8, 1.2]` 改为 `[3.5, 2.5, 1.8]`（低/中/高坡全程示意时长）。
- `baseDuration()` 不再乘以 `targetProgress()`，全程统一用完整时长。
- `animate()` / `animateManual()` 以 `stopAt = Math.sqrt(target)` 控制停止点，使位移曲线保持 `s ∝ t²`。
- 手动模式保留反应误差叠加，再测一次与数据表逻辑不变。

**验证（node 模拟，无手动反应误差）**

| 档位 | t₁(s) | t₂(s) | t₂/t₁ | v₁(cm/s) | v₂(cm/s) | v₃(cm/s) | v₃/v₂ |
|------|-------|-------|-------|----------|----------|----------|-------|
| 低坡 | 3.500 | 2.475 | 0.707 | 28.57 | 20.20 | 48.77 | 2.41 |
| 中坡 | 2.500 | 1.768 | 0.707 | 40.00 | 28.28 | 68.28 | 2.41 |
| 高坡 | 1.800 | 1.273 | 0.707 | 55.56 | 39.28 | 94.84 | 2.41 |

- 默认中坡 `t₂/t₁ = 0.707 ∈ [0.65, 0.75]` ✅
- `v₃/v₂ = 2.41 ≥ 1.4` ✅
- 动画仍属示意，不声称物理精确。

## F3 quiz.html：频闪图 B 球归位

**修法**
- 刻度尺左端 `x=40` 对应 0 cm（C 球），右端 `x=440` 对应 100 cm（A 球）。
- B 球由 `cx=315` 移至 `cx=340`，即 75 cm / 75% 位置。
- B 球上方字母标签、下方 `00:01.00` 时刻标注同步右移至 `x=340`。
- A、C 球及判定逻辑（权威值 25/75/50）未动。

## F4 exp-sensor.html：滑块量程恢复 0~1.00 m

**修法**
- 用 `CENTER_MIN_X = 84`（滑块最小处小车中心）到 `CENTER_MAX_X = 504`（滑块最大处小车中心）的 420 px 线性映射 0~1.00 m。
- `pxToM()` / `mToPx()` 改用新映射。
- 轨道刻度重新按 `0~100 cm` 均匀布满滑块全程；轨道矩形同步改为 `x=84 width=420`。
- 脉冲计时公式仍为 `t = 2s / 340`（即 `inputT = m * 2 / 340 * 1000`），物理正确。
