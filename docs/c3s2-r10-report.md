# c3s2 R10 修复报告（2026-09-26）

蔡总反馈四项，处理如下：

1. **点燃后橙色箭头**：非 bug，是教学指示「热传递方向箭头」（heatArrow）——加热时橙色指向水浴表示热量流入；凝固模式放热时变蓝反向。是否保留待蔡总定夺（本轮未动）。
2. **删烧杯嘴**：exp-melt-core.js 删 spoutMat/spoutTrough/侧翼全部代码（grep spout 零残留），杯口恢复干净圆口。侧视/俯视目视无残影。
3. **面板样品物态**：一直在（首行 chip），但标签只写「物态」，R9 新增「水的物态」行后易误解为样品物态丢了。标签改「样品物态」，两行并列清晰。
4. **图表 y 轴**：exp-melt-chart.js computeYRange 改固定 {yMin:-15, yMax:100, step:20}，刻度 0/20/40/60/80/100 + 底部 -15 单独一条；不再随数据扩缩。熔点虚线/窗口滑动逻辑未动。

验收（主会话 live 复测）：标签=样品物态；scene 无 spout 对象；加热 600 步水浴钳 100、气泡正常；图表顶 100 底 -15 刻度清晰；console 零报错；node --check 三文件通过。
改动文件：exp-melt-core.js、exp-melt.html、exp-melt-chart.js。
