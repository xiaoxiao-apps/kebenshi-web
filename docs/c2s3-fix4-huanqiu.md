# c2s3 圜丘补单2 收尾验证报告

## 验收项现状

1. **站台/小人/天心石**：站台 `platformY=0.72H`，宽度 390px~1200px 完整可见；小人脚底 y=台面顶 y，差 0px（<4px）；天心石在 `(W/2, py+ph/2)` 居中。
2. **文字标注**：左上角「半径约 11.5 m」「高出地面约 5 m」左对齐两行，不与小人/站台/栏板/波前重叠；「汉白玉栏板」移到左侧栏板上方天空区，宽屏/手机视口均完整可见。
3. **反射回程过程感**：反射波从栏板反射点出发，朝 `W/2` 方向以 60° 扇形传播；实测 reflect 状态后半径从 129 递增到 594。
4. **外发波消散**：进入 reflect 后原外发弧继续原速外扩、alpha 渐淡，出界后移除。
5. **保留项**：连点多组波、无重置键、`reflectTargetX=W/2`、读数 pills、info 卡、FullscreenHelper、返回按钮均保留。\n## 改动

- `exp-huanqiu.js`：仅调整 `汉白玉栏板` 文字位置到左侧栏板上方天空区，避免窄视口截断；其余实现保持不变。

## 自查结果

- `node --check exp-huanqiu.js`：通过
- HTML 三重自查：DOCTYPE 正确、`</html>` 唯一、首尾正确
- `browser errors`：0 条
- 站台像素 y∈[0.6H,0.95H]：存在
- 小人脚底与台面顶差：0px（<4px）
- 反射波半径增长：129 → 594（1.5s 间隔采样递增）
- 宽屏截图：`/tmp/huanqiu-wide.png`
- 手机截图：`/tmp/huanqiu-mobile.png`
